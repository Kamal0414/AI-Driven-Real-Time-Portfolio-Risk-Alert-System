{
  "name": "@prr/market-data-service",
  "version": "0.1.0",
  "private": true,
  "description": "Market Data Service — simulates live price streaming, publishes PriceUpdated events",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prr/shared": "*",
    "@aws-sdk/client-dynamodb": "^3.629.0",
    "@aws-sdk/lib-dynamodb": "^3.629.0",
    "@aws-sdk/client-eventbridge": "^3.629.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "typescript": "^5.5.3"
  }
}
