{
  "name": "@prr/ai-insight-service",
  "version": "0.1.0",
  "private": true,
  "description": "AI Insight Service — generates LLM-based portfolio commentary from risk breaches",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@prr/shared": "*",
    "@aws-sdk/client-dynamodb": "^3.629.0",
    "@aws-sdk/lib-dynamodb": "^3.629.0",
    "@aws-sdk/client-eventbridge": "^3.629.0",
    "uuid": "^9.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/uuid": "^9.0.8",
    "typescript": "^5.5.3"
  }
}
